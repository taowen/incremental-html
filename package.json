{
    "name": "incremental-html",
    "version": "1.0.0",
    "private": true,
    "devDependencies": {
        "typescript": "^4.5.5",
        "npm-run-all": "^4.1.5"
    },
    "files": [],
    "scripts": {
      "build": "npm-run-all -p build:*",
      "build:cjs": "tsc -b cjs.tsconfig.json --watch",
      "build:esm": "tsc -b esm.tsconfig.json --watch"
    }
}